---
description: Run the feature-port Playbook for an evidence-first target-native capability port
agent: build
---

Execute the stored `feature-port` Playbook for:

$ARGUMENTS

Keep OpenCode `build` as primary controller and materialize one Step at a time. Exact source and target identities are mandatory before implementation. Port portable behavior and proven invariants, not source filenames/runtime topology. Stop if target ownership requires a new architectural decision.
