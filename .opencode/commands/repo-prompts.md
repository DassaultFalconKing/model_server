---
description: Suggest high-value copy/paste prompts for this repository
agent: build
---

Inspect this repository and propose the next high-value prompts for review,
documentation, profile generation, verification and current-doc research. Stay
on OpenCode's primary `build` agent so the native provider-specific controller
prompt for the selected model remains in effect.

Optional focus: $ARGUMENTS

You may Task `repo-prompt-advisor` for a bounded evidence pass. Return prompts
I can paste directly into OpenCode. Order them by expected value and tie every
recommendation to repository evidence.
