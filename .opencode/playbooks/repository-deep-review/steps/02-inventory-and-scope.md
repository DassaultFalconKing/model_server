# Step: inventory and scope

Use the exact `target_identity` from the previous Step. Read project authorities and call deterministic repository inventory before broad reading.

Load `repository-deep-review` for each bounded component/surface rather than treating this Step as one giant inspection prompt. Build a compact map of entry points, ownership, data/control flow, persistence, external integrations, tests/CI, and the requested scope.

Return only `review_map`: bounded component list, exact candidate paths/symbols, cross-component questions, and explicit unreviewed areas. Generated graphs are navigation aids, not evidence.
