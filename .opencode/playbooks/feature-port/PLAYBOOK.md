# Feature Port Playbook

Turns one source capability into a target-native CodeSleuth change without copying source-specific ownership or runtime assumptions. The parent reads the manifest only; each Step is materialized separately.

Use `/playbook feature-port <source/target task>` or `/repo-port`.
