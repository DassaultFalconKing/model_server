# Repository Map Playbook

Builds or refreshes one bounded RepositoryContextProjection and optional Mermaid view without loading the whole review protocol. Use `/playbook repository-map <scope>` or `/repo-map`.
