# Protected Capability Assessment Playbook

Runs a contract-impact or registry-maintenance assessment without loading the former broad registry Skill. It composes exact identity, registry query, contract triangulation, forbidden-regression audit, and dependency closure as separate Steps.

Use `/playbook protected-capability-assessment <query/diff>` or `/repo-contracts` for broad assessment. For one simple registry lookup, invoke `/protected-capability-registry` directly.
