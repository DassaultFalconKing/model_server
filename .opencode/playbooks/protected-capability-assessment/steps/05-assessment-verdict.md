# Step: assessment verdict

Summarize the exact target, matched contracts, drift, relevant forbidden regressions, affected reverse dependency closure, and the gate implied by the requested claim.

For an ordinary development candidate, distinguish invariant core + affected closure + new-feature proof. For SIB2, accepted integration heads, RCs, and releases, state that dependency selection does not replace the full canonical acceptance profile.

Do not promote lifecycle status or claim EHA from historical `protected_at` evidence. Return explicit `PASS`, `FAIL`, or `UNPROVEN` only for the assessment question actually asked.
