# Repository contract bootstrap

Brownfield onboarding Playbook for repositories whose contracts exist in code, documentation, tests and operator behavior but have not yet been canonicalized into a Protected Capability Registry.

The Playbook discovers and triangulates candidates first, stops for explicit user adjudication, and materializes only adopted candidates. Discovery never confers SIB acceptance or `PROTECTED` status.
