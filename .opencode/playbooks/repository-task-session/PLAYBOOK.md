# Repository Task Session Playbook

Runs the exact task-specific continuation prompt after repository discovery has established a baseline. Replace `<TASK>` in the materialized Step with the concrete task. Use `/playbook repository-task-session`.
