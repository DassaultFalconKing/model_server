# GEMMAMONSTER Claude bootstrap

Before your first tool call, read `/AGENTS.md` completely and obey its `BEFORE THE FIRST TOOL CALL` section.

Then read the canonical authority and negative-contract documents named there.

Do not begin repository inspection, Git operations, search, shell commands, builds, tests, or edits before doing so.

`AGENTS.md` and the canonical documents it references are authoritative. This file does not override them and must remain a thin adapter.
